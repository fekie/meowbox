use super::State;

// Light Ring
impl State {
    pub(super) fn control_light_ring(&mut self) {
        // choose which method to run based on which function is
        // currently running
    }

    fn setup_light_ring(&mut self) {}

    fn execute_light_ring(&mut self) {}
}
